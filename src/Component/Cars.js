import React, { useContext } from 'react'

const Cars = () => {
// const theme = useContext(themeContext)

  return (
    <div>
        <h1 style={{ color:"red" , fontSize:"80px" , fontFamily:"cursive" , fontStyle:"italic"}}>I like to drive a car</h1>
        {/* current theme: {theme} */}
    </div>
  )
}

export default Cars